# YourTrackOrderBot / TrackOrderPro

Telegram-бот для продавцов маркетплейсов: отслеживание заказов + шаблоны ответов и ночной автоответ.

## Что умеет MVP

- Команда `/start` — показывает меню и краткую инструкцию.
- `/track` — отслеживание статуса заказа.
- `/templates` — быстрые шаблоны ответов (инлайн-кнопки).
- `/night` — ночной режим с автоответом "статус утром".
- `/pay` — кнопка оплаты 300₽/мес через YooKassa.

## Как запустить локально

1. Установить Python 3.11+.
2. Клонировать репозиторий:
   ```bash
   git clone https://github.com/stasyk411/YourTrackOrderBot-.git
   cd YourTrackOrderBot-
   Создать виртуальное окружение и установить зависимости:
   ```

bash
python -m venv venv

# Для Windows:

venv\Scripts\activate

# Для macOS/Linux:

source venv/bin/activate
pip install -r requirements.txt
Создать файл .env на основе .env.example и добавить токен бота:

text
TELEGRAM*BOT_TOKEN=ваш*токен_бота
Запустить бота:

bash
python bot.py
Архитектура проекта
text
YourTrackOrderBot/
├── bot.py # Главная точка входа
├── app/ # Модули приложения
│ ├── main.py # Инициализация бота
│ ├── routes.py # Регистрация хендлеров
│ └── **init**.py
├── handlers/ # Обработчики команд
│ ├── start.py # /start
│ ├── track.py # /track
│ ├── templates.py # /templates
│ └── night.py # /night
├── core/ # Бизнес-логика
│ ├── database.py # Работа с БД
│ └── config.py # Конфигурация
├── data/ # Данные
│ └── templates_database.py # Шаблоны ответов
└── utils/ # Вспомогательные функции
Исправление проблем с упоминаниями площадок
Для соответствия правилам маркетплейсов в коде проекта:

Убраны все прямые упоминания "WB", "Wildberries", "Ozon"

Использованы нейтральные формулировки: "маркетплейс", "площадка", "личный кабинет"

Шаблоны ответов универсальны для любых площадок

Планы развития
Реальный трекинг — интеграция с API служб доставки

Интеллектуальный подбор — умный подбор шаблонов по контексту

Пакетная обработка — ответы на несколько заказов сразу

Аналитика — статистика по запросам и проблемам
**Что исправлено:**

1. ✅ `"Telegram-бот для селлеров WB/Ozon:"` → `"Telegram-бот для продавцов маркетплейсов:"`
2. ✅ `"заглушка статуса заказа (позже будет реальный WB API)"` → `"отслеживание статуса заказа"`
3. ✅ Добавлен раздел об исправлении проблем с упоминаниями
