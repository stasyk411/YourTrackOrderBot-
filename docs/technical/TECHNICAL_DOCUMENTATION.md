# 📚 **ДОКУМЕНТ 3: ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ (для одного разработчика)**

## 🎯 **ЦЕЛЬ ПРОЕКТА:**

- **Пользователи:** до 1,000
- **Разработчик:** 1 человек (вы)
- **Инфраструктура:** 1 VPS, SQLite/PostgreSQL
- **Сложность:** минимальная, поддерживаемая одним человеком
- **Бюджет:** ~$135/год

## 1. 🏗️ **МИНИМАЛЬНАЯ АРХИТЕКТУРА:**

```

your-bot/
├── app/ # Основное приложение
│ ├── main.py # Точка входа (обязательно с sys.path!)
│ └── routes.py # Маршруты
├── core/ # Ядро системы
│ ├── config.py # Конфигурация (.env)
│ └── database.py # База данных
├── handlers/ # Обработчики Telegram
│ ├── start.py # /start
│ ├── track.py # /track
│ └── templates.py # /templates
├── data/ # Данные
│ └── templates_database.py # Шаблоны ответов
├── utils/ # Вспомогательные функции
├── .venv/ # Виртуальное окружение (ОБЯЗАТЕЛЬНО!)
├── .env # Переменные окружения (НЕ коммитить!)
├── requirements.txt # Зависимости
├── README.md # Инструкция запуска
└── bot.py # Точка входа (запускает app/main.py)

```

## 2. 📝 **КОММЕНТАРИИ В КОДЕ (достаточно минимума):**

```python
# handlers/track.py
def handle_track(bot, message):
    """
    Обрабатывает /track [номер_заказа]

    Пример: /track 123456789
    Возвращает статус заказа
    """
    # Простой код, понятный через месяц
```

## 3. 🚀 **ПРОСТОЙ ДЕПЛОЙ (для VPS $5-10/месяц):**

```bash
# На сервере (Ubuntu):
1. sudo apt update && sudo apt upgrade -y
2. sudo apt install python3.11 python3.11-venv -y
3. git clone ваш-репозиторий /opt/your-bot
4. cd /opt/your-bot
5. python3.11 -m venv .venv
6. source .venv/bin/activate
7. pip install -r requirements.txt
8. cp .env.example .env  # Заполнить TELEGRAM_BOT_TOKEN

# Systemd сервис (/etc/systemd/system/telegram-bot.service):
[Unit]
Description=Telegram Bot
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/your-bot
Environment="PATH=/opt/your-bot/.venv/bin"
ExecStart=/opt/your-bot/.venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## 4. 📊 **МИНИМАЛЬНЫЙ МОНИТОРИНГ:**

```bash
# ДОСТАТОЧНО:
1. Логи: sudo journalctl -u telegram-bot.service -f
2. Доступность: проверять раз в день работает ли бот
3. Ошибки: просматривать логи раз в неделю
4. Бэкапы: копировать папку /opt/your-bot раз в неделю
```

## 5. 🔄 **ПРОСТОЙ ПРОЦЕСС РАЗРАБОТКИ:**

```bash
# 1. Создать ветку для новой фичи
git checkout -b feature/новая-фича

# 2. Работать локально (в .venv!)
.venv\Scripts\activate  # Windows
python bot.py  # Тестировать

# 3. Закоммитить с понятным сообщением
git add .
git commit -m "feat: добавил команду /track"

# 4. Влить в main и задеплоить
git checkout main
git merge feature/новая-фича
git push

# 5. На сервере обновить
ssh ваш-сервер
cd /opt/your-bot
git pull
sudo systemctl restart telegram-bot.service
```

## 6. 💰 **БЮДЖЕТ И РЕСУРСЫ:**

```
VPS: $5-10/месяц (1GB RAM, 1 CPU достаточно)
Домен: $15/год (опционально, можно без)
Telegram Bot: бесплатно
Хостинг базы данных: не нужно (SQLite/локальный PostgreSQL)
Итого: $60-135/год
```

## 7. ⚠️ **ЧЕГО НЕ ДЕЛАТЬ (экономия времени):**

- ❌ НЕ делать микросервисы
- ❌ НЕ настраивать Kubernetes/Docker swarm
- ❌ НЕ делать сложные CI/CD (достаточно git pull)
- ❌ НЕ писать 100% тестов (достаточно основных сценариев)
- ❌ НЕ создавать отдельный сервер мониторинга
- ❌ НЕ делать сложную балансировку нагрузки

## 8. ✅ **ЧТО ДЕЛАТЬ (оптимально):**

- ✅ Держать код простым и понятным
- ✅ Использовать SQLite для разработки
- ✅ Перейти на PostgreSQL если будет > 500 пользователей
- ✅ Делать бэкапы раз в неделю
- ✅ Проверять логи раз в неделю
- ✅ Обновлять зависимости раз в месяц
- ✅ Следовать PROJECT_GUIDE.md для стандартов

## 9. 📈 **ЕСЛИ ВЫРАСТЕТ ДО 1,000 ПОЛЬЗОВАТЕЛЕЙ:**

```
1. VPS: апгрейд до $20/месяц (2GB RAM, 2 CPU)
2. База данных: PostgreSQL вместо SQLite
3. Бэкапы: каждый день вместо недели
4. Мониторинг: добавить uptime проверку
5. Архитектура: остаётся прежней (до 5,000 пользователей ок)
```

---

## 🎯 **ГЛАВНОЕ ПРАВИЛО ДЛЯ ОДНОГО РАЗРАБОТЧИКА:**

**"Держи всё максимально простым. Добавляй сложность только когда текущее решение перестаёт работать.  
Лучше простой работающий проект, чем сложный незаконченный."**

---

**© Документ адаптирован под одного разработчика и малый проект**
**Версия: 2.0 (упрощённая) | Дата: 2026-02-08 | Статус: ОБЯЗАТЕЛЬНЫЙ**
