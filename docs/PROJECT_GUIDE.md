# 🚀 **PROJECT_GUIDE.md - СТАНДАРТ РАЗРАБОТКИ**

## 📋 **БЫСТРЫЙ СТАРТ (для ИИ и разработчика)**

### **🎯 КАК РАБОТАТЬ В ЭТОМ ПРОЕКТЕ:**

```
1. ИИ запускает: scripts/smart-start.ps1
2. Проверяем: (.venv) должно быть в начале строки
3. Работаем: одна команда → выполнение → отчёт
4. Коммитим: только после проверки импортов
```

### **⚡ КОРОТКИЕ КОМАНДЫ ДЛЯ ПАМЯТИ:**

```powershell
# Активация .venv (если не видно (.venv))
.venv\Scripts\activate

# Исправление кодировки (если кракозябры)
[System.Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# Базовая проверка импортов
python -c "import sys; import os; sys.path.insert(0, os.path.dirname(os.getcwd())); print('✅ Пути настроены')"

# Проверка проекта
python scripts/smart-start.ps1
```

---

## 🎯 **ЧАСТЬ 1: ОСНОВНЫЕ ПРАВИЛА (ДЛЯ ИИ)**

### **1.1 СТИЛЬ РАБОТЫ С РАЗРАБОТЧИКОМ:**

- **Разработчик новичок** - объяснять каждый шаг
- **Одна команда = одно действие** - не давать несколько шагов сразу
- **Ждать выполнения** - после команды ждать отчёт разработчика

### **1.2 ФОРМАТ КОМАНД:**

```powershell
# ❌ НЕПРАВИЛЬНО:
"Создайте файл и добавьте код"

# ✅ ПРАВИЛЬНО:
# КОМАНДА:
New-Item -Path src/main.py -ItemType File

# ЧТО ДЕЛАЕТ:
# Создаёт файл main.py в папке src

# РЕЗУЛЬТАТ:
# Файл будет создан, можно редактировать

# ВАШИ ДЕЙСТВИЯ:
# 1. Скопируйте команду
# 2. Выполните в PowerShell
# 3. Сообщите результат
```

### **1.3 ПРОВЕРКИ ПЕРЕД ДЕЙСТВИЯМИ:**

1. ✅ .venv активирован? (видно (.venv))
2. ✅ Кодировка UTF-8? (русский текст читается)
3. ✅ Импорты работают? (базовая проверка)
4. ✅ Не в main ветке? (git branch --show-current)

---

## 🎯 **ЧАСТЬ 2: ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ**

### **2.1 ВИРТУАЛЬНОЕ ОКРУЖЕНИЕ (.venv):**

```powershell
# ВСЕГДА должно быть:
(.venv) PS C:\Ваш\Путь\Проекта>

# КАК ПРОВЕРИТЬ:
Test-Path ".venv"  # Должно вернуть True
$env:VIRTUAL_ENV   # Должно быть не пусто

# ЕСЛИ НЕТ .venv:
python -m venv .venv
.venv\Scripts\activate
```

### **2.2 КОДИРОВКА UTF-8:**

```powershell
# ПРОБЛЕМА: кракозябры вместо русского текста
# РЕШЕНИЕ:
[System.Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# ПРОВЕРКА:
[System.Console]::OutputEncoding.EncodingName  # Должно быть "UTF-8"
```

### **2.3 СТРУКТУРА ПРОЕКТА:**

```
project/
├── src/           # Исходный код
│   ├── __init__.py
│   └── main.py    # Главный файл (ДОЛЖЕН иметь настройку sys.path!)
├── .venv/         # Виртуальное окружение
├── docs/          # Документация
├── scripts/       # Скрипты
├── tests/         # Тесты
├── .env           # Переменные окружения (НЕ КОММИТИТЬ!)
├── .gitignore     # Игнорируемые файлы
├── requirements.txt # Зависимости
└── README.md      # Описание
```

### **2.4 ГЛАВНЫЙ ФАЙЛ (src/main.py):**

```python
# ОБЯЗАТЕЛЬНО в начале каждого main.py:
import sys
import os

# Добавляем корень проекта в путь поиска модулей
PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, PROJECT_ROOT)

# Теперь можно импортировать свои модули
from src.core.config import Config
from src.utils.helpers import helper_function
```

---

## 🎯 **ЧАСТЬ 3: GIT И ВЕТКИ**

### **3.1 ВЕТКИ:**

```powershell
# ПРОВЕРКА ТЕКУЩЕЙ ВЕТКИ (первая команда в чате):
git branch --show-current

# ❌ НЕ РАБОТАТЬ В:
main
master

# ✅ РАБОТАТЬ В:
feature/...
bugfix/...
refactor/...
hotfix/...
```

### **3.2 КОММИТЫ:**

```powershell
# ПРОВЕРКА СТАТУСА:
git status

# ДОБАВЛЕНИЕ ИЗМЕНЕНИЙ:
git add .

# СОЗДАНИЕ КОММИТА:
git commit -m "тип: краткое описание"

# ТИПЫ КОММИТОВ:
feat:     Новая функция
fix:      Исправление ошибки
docs:     Документация
style:    Форматирование кода
refactor: Рефакторинг
test:     Тесты
chore:    Обновление зависимостей/настроек
```

### **3.3 .gitignore (ОБЯЗАТЕЛЬНО):**

```
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual Environment
.venv/
venv/
env/

# Environment variables
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

---

## 🎯 **ЧАСТЬ 4: РАБОЧИЙ ПРОЦЕСС**

### **4.1 НАЧАЛО РАБОТЫ:**

```powershell
# ИИ ВЫПОЛНЯЕТ САМ:
.\scripts\smart-start.ps1

# РАЗРАБОТЧИК ПРОВЕРЯЕТ:
# 1. Видно ли (.venv) в начале строки?
# 2. Русский текст отображается правильно?
# 3. Какая текущая ветка? (не main)
```

### **4.2 ПЕРЕД КАЖДЫМ ИЗМЕНЕНИЕМ:**

```powershell
# БАЗОВАЯ ПРОВЕРКА:
python -c "
import sys
import os
try:
    sys.path.insert(0, os.path.dirname(os.getcwd()))
    # Импорт главного модуля проекта
    from src.main import main
    print('✅ Импорты работают')
except Exception as e:
    print(f'❌ Ошибка импорта: {e}')
"
```

### **4.3 ПЕРЕД КАЖДЫМ КОММИТОМ:**

1. ✅ Запустить проверку импортов
2. ✅ Проверить что нет упоминаний WB/Ozon (если Telegram бот)
3. ✅ Обновить requirements.txt если менялись зависимости
4. ✅ Убедиться что .env не добавляется в git

---

## 🎯 **ЧАСТЬ 5: РЕШЕНИЕ ПРОБЛЕМ**

### **5.1 "No module named ..."**

```powershell
# ПРИЧИНЫ:
# 1. .venv не активирован
# 2. Нет настройки sys.path в главном файле
# 3. Запуск не из корня проекта

# РЕШЕНИЕ:
# 1. .venv\Scripts\activate
# 2. Проверить src/main.py (должны быть строки sys.path)
# 3. cd в корень проекта
```

### **5.2 Кракозябры в консоли**

```powershell
# РЕШЕНИЕ:
[System.Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# ЕСЛИ НЕ ПОМОГАЕТ:
# 1. В VS Code: правый нижний угол → UTF-8
# 2. В PowerShell: chcp 65001
```

### **5.3 Зависимости не устанавливаются**

```powershell
# 1. АКТИВИРОВАТЬ .venv:
.venv\Scripts\activate

# 2. ОБНОВИТЬ pip:
python -m pip install --upgrade pip

# 3. УСТАНОВИТЬ:
pip install -r requirements.txt

# 4. СОХРАНИТЬ НОВЫЕ:
pip freeze > requirements.txt
```

---

## 🎯 **ЧАСТЬ 6: БЫСТРЫЕ КОМАНДЫ**

### **6.1 ДЛЯ РАЗРАБОТЧИКА:**

```powershell
# Активировать .venv
.venv\Scripts\activate

# Проверить проект
python .\scripts\smart-start.ps1

# Проверить импорты
python -c "import sys; import os; sys.path.insert(0, os.path.dirname(os.getcwd())); from src.main import main; print('✅ OK')"

# Обновить зависимости
pip freeze > requirements.txt
```

### **6.2 ДЛЯ ИИ:**

```powershell
# Начать чат:
Write-Host "🚀 Запускаю smart-start..." -ForegroundColor Cyan
.\scripts\smart-start.ps1

# После проверки:
Write-Host "📋 Проект готов к работе. Жду вашу команду." -ForegroundColor Green
```

---

## 📞 **ЭКСТРЕННАЯ ПОМОЩЬ**

### **ЕСЛИ ВСЁ СЛОМАЛОСЬ:**

```powershell
# 1. Выйти из Python (Ctrl+C)
# 2. Деактивировать .venv (deactivate если есть)
# 3. Удалить .venv и создать заново:
Remove-Item -Path .venv -Recurse -Force
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 4. Проверить:
.\scripts\smart-start.ps1
```

### **ЕСЛИ ИИ НЕ СЛЕДУЕТ ПРАВИЛАМ:**

```
Напомните: "Следуй PROJECT_GUIDE.md, запусти smart-start.ps1"
```

---

## 🎯 **ФИНАЛЬНОЕ ПРАВИЛО:**

**"Сначала scripts/smart-start.ps1 → потом работа.  
Сначала проверка импортов → потом изменения.  
Всегда (.venv) → никогда системный Python."**

---

_Этот документ должен находиться в `docs/PROJECT_GUIDE.md`  
Скрипт автоматической проверки в `scripts/smart-start.ps1`_

```

```

## 📚 **5 ОБЯЗАТЕЛЬНЫХ ДОКУМЕНТОВ ДЛЯ РАБОТЫ:**

### **1. 📋 ДОКУМЕНТ 1: Продуктовый чек-лист**

**Когда использовать:** ПЕРЕД началом кодинга новой функции
**Файл:** `docs/products/PRODUCT_CHECKLIST.md`
**Что проверять:** Цель, аудитория, монетизация, MVP

### **2. 🎨 ДОКУМЕНТ 2: UX/UI гайдлайны**

**Когда использовать:** ПРИ проектировании интерфейса
**Файл:** `docs/ux-ui/UX_UI_GUIDELINES.md`
**Что проверять:** Mobile-first, когнитивная нагрузка, ошибки

### **3. 📚 ДОКУМЕНТ 3: Техническая документация**

**Когда использовать:** ПРИ технических решениях
**Файл:** `docs/technical/TECHNICAL_DOCUMENTATION.md`
**Что проверять:** Архитектура, деплой, мониторинг

### **4. 🔒 ДОКУМЕНТ 4: Правила безопасности**

**Когда использовать:** ПРИ работе с данными/оплатой
**Файл:** `docs/security/SECURITY_RULES.md`
**Что проверять:** Данные, платежи, юридические требования

### **5. 🔗 ДОКУМЕНТ 5: Интеграции**

**Когда использовать:** ПРИ добавлении внешних API
**Файл:** `docs/integrations/SIMPLE_INTEGRATIONS.md`
**Что проверять:** Простые интеграции, минимум зависимостей

---

## 🎯 **ПРАВИЛО ДЛЯ ИИ:**

**ПЕРЕД любым действием задать вопрос: "Какой документ из 5 нужно проверить для этой задачи?"**
